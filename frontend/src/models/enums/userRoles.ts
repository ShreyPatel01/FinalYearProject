export enum projectRoles {
  ADMIN = "ADMIN",
  USER = "USER",
  MOD = "MODERATOR",
  CLIENT = "CLIENT"
}

export enum taskRoles {
  CREATOR = "CREATOR",
  ASSIGNEE = "ASSIGNEE",
}

export enum chatRoles {
  ADMIN = "ADMIN",
  MODERATOR = "MODERATOR",
  MEMBER = "MEMBER",
  CLIENT = "CLIENT",
}

export enum userRoles {
  USER = "USER",
  CLIENT = 'CLIENT'
}